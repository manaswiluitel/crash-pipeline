jobs:
  deploy:
    name: Deploy Pipeline to Azure
    runs-on: ubuntu-latest
    env:
      AZURE_HOST: ${{ secrets.AZURE_HOST }}
      AZURE_USER: ${{ secrets.AZURE_USER }}
      AZURE_SSH_KEY: ${{ secrets.AZURE_SSH_KEY }}
      PROJECT_PATH: ${{ secrets.PROJECT_PATH }}
    steps:
      - name: Set up SSH key
        run: |
          echo "$AZURE_SSH_KEY" > key.pem
          chmod 600 key.pem

      - name: Deploy latest version on Azure VM
        run: |
          ssh -i key.pem -o StrictHostKeyChecking=no "$AZURE_USER@$AZURE_HOST" "
            set -e
            cd \"$PROJECT_PATH\"
            echo '🛑 Stopping any running services...'
            docker compose down
            echo '🚚 Pulling latest changes from Git...'
            git pull
            echo '🛠️ Building new container images...'
            docker compose build
            echo '🚀 Starting all services...'
            docker compose up -d
            echo '✅ Deployment commands finished.'
          "

      - name: Smoke test – HTTP health checks
        # We will try to run the smoke tests. If they fail, the next step will run.
        continue-on-error: true
        id: smoke_test
        run: |
          ssh -i key.pem -o StrictHostKeyChecking=no "$AZURE_USER@$AZURE_HOST" '
            set -e
            cd "$PROJECT_PATH"
            echo "⏳ Waiting for services to stabilize..."
            sleep 30
            echo "🔍 Checking Streamlit health..."
            curl -f http://localhost:8501/_stcore/health
            # (The other curl checks are here, but we will focus on the first failure)
          '

      # --- THIS IS THE NEW DEBUGGING STEP ---
      # This step will only run if the 'Smoke test' step above fails.
      - name: Debug Docker state on failure
        if: steps.smoke_test.outcome == 'failure'
        run: |
          echo "🔬 Smoke test failed. Collecting debug information from the VM..."
          ssh -i key.pem -o StrictHostKeyChecking=no "$AZURE_USER@$AZURE_HOST" '
            cd "$PROJECT_PATH"
            echo "--- Docker container status (docker ps -a) ---"
            docker ps -a
            echo ""
            echo "--- Logs for the Streamlit container ---"
            # This command will print the logs, which should contain the Python error.
            docker compose logs --tail="50" streamlit
          '
          # This line ensures the whole workflow fails, even though we collected logs.
          exit 1
