# .env.sample - Environment Variable Template
#
# Copy this file to .env and fill in the required values.
# This file defines the configuration for the entire pipeline.
# Do NOT commit the .env file to version control.

# ===== RabbitMQ =====
# Set your desired username and password for RabbitMQ.
# For local development, 'guest' is a common default.
RABBIT_USER=guest
RABBIT_PASS=guest

# Default ports for RabbitMQ service and management UI.
RABBIT_PORT=5672
RABBIT_UI=15672

# The connection URL used by services to connect to RabbitMQ.
# Docker Compose will automatically substitute the variables above.
RABBITMQ_URL=amqp://${RABBIT_USER}:${RABBIT_PASS}@rabbitmq:${RABBIT_PORT}/

# ===== MinIO =====
# Set your desired access key (user) and secret key (password) for MinIO.
# For local development, 'admin' and 'admin123' are simple examples.
MINIO_USER=admin
MINIO_PASS=admin123

# Default ports for MinIO API and web console.
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# The endpoint used by services to connect to the MinIO API.
MINIO_ENDPOINT=minio:${MINIO_API_PORT}
MINIO_SSL=false

# ===== Socrata API =====
# Public endpoint for the Chicago Data Portal API.
SOCRATA_BASE=https://data.cityofchicago.org

# ===== Storage & Queue Names =====
# Names for the MinIO buckets and RabbitMQ queues used in the pipeline.
RAW_BUCKET=raw-data
TRANSFORM_QUEUE=transform
XFORM_BUCKET=transform-data
EXTRACT_QUEUE=extract
CLEAN_QUEUE=clean

# ===== Health Check Ports =====
# Ports used by the services to expose their health check endpoints.
EXTRACTOR_HEALTH_PORT=8001
TRANSFORMER_HEALTH_PORT=8002
CLEANER_HEALTH_PORT=8003
